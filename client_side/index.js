import React from "react";
import  ReactDOM  from "react-dom";
import App from './App';

ReactDOM.render(<App/>,document.getElementById('root')); //connect
//display the specified HTML code inside the specified HTML element.